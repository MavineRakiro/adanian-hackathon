import React from "react";
import styled from "styled-components";

const Overlay = styled.div`
  position: relative;
  width: 1728px;
  height: 1117px;
  background: rgba(201, 195, 195, 0.69);
  backdrop-filter: blur(20px);
`;

const Modal = styled.div`
  
`;

const SignUpModal = () => {
  return (
    <div>
      Sign up Modal 
    </div>
  );
};

export default SignUpModal;
